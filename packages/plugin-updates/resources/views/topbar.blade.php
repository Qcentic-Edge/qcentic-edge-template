{{--
    What the panel's topbar render hook renders. Nothing but a mount point: the
    decision to render at all is made before this view is reached, and what is
    owed is read inside the component from the report.
--}}
@livewire('plugin-updates.notice')
