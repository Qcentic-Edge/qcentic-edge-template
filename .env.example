# Copy to `.env` next to docker-compose.*.yml.
# Compose interpolates `name:` from this file (or the shell), NOT from
# `--env-file .env.docker.dev`. Clones: set a unique name. Do not edit YAML.
COMPOSE_PROJECT_NAME=qcentic-edge-template-dev
